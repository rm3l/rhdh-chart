# Collect secrets (sanitized) and cluster-level info
gather:
  withSecrets: true
  clusterInfo: true
  logLevel: "INFO"
  extraEnvVars:
    - name: CMD_TIMEOUT
      value: "5"
    - name: LOG_LEVEL
      value: "debug"

{{- if and .Values.serverlessOperator.enabled .Values.serverlessOperator.createNamespace }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.serverlessOperator.subscription.namespace }}
  annotations:
    "helm.sh/resource-policy": keep
{{- end }}


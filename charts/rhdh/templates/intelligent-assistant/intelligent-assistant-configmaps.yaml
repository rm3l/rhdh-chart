{{- $intelligentAssistant := include "rhdh.intelligentAssistant" . | fromYaml -}}
{{- if $intelligentAssistant.enabled }}
{{- $first := true }}
{{- range $key := list "stack" "profile" }}
{{- $entry := index $intelligentAssistant.config $key }}
{{- if not $entry.existingConfigMap.name }}
{{- if not $first }}
---
{{- end }}
{{- $first = false }}
{{- $file := include "rhdh.intelligentAssistant.configFile" $key }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rhdh.intelligentAssistant.configMapName" (dict "root" $ "key" $key "entry" $entry) }}
  labels:
    {{- include "rhdh.labels" $ | nindent 4 }}
  {{- with $.Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
  {{- end }}
data:
  {{ $file }}: |
{{ $.Files.Get (printf "files/intelligent-assistant/%s" $file) | nindent 4 }}
{{- end }}
{{- end }}
{{- end }}
